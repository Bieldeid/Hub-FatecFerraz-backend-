const trabalhoRepository = require('../repositories/trabalhoRepository');
const rubricaRepository = require('../repositories/rubricaRepository');

module.exports = {
    async createRubrica(id, professor_id, curso_id,ano,semestre,titulo,descricao){

    }
}
